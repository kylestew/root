import { Vec2 } from '../geo/types';
import { Polygon } from '../geo/index';
export declare function handDrawnCircle(pos: Vec2, r: number, noiseMult?: number, noiseScale?: number, steps?: number, zStep?: number): Polygon;
