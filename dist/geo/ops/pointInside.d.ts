import { Shape, Vec2 } from '../types';
/**
 * Returns true if point `pt` is inside the given shape.
 *
 * @param shape
 * @param pt
 */
export declare function pointInside(shape: Shape, pt: Vec2): boolean;
