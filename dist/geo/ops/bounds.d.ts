import { Shape } from '../types';
import { Rectangle } from '../index';
/**
 * Computes and returns bounding rect/box for the given shape.
 *
 * @param shape
 */
export declare function bounds(shape: Shape): Rectangle;
