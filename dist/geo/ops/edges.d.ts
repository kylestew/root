import { Shape } from '../types';
/**
 * Extracts the edges of given shape's boundary and returns them as an iterable
 * of vector pairs.
 *
 * @param shape
 */
export declare function edges(shape: Shape): any[];
